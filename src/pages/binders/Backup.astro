---
---

<html lang="en">
	<head>
		<meta charset="utf-8" />
		<link rel="icon" type="image/svg+xml" href="/Moltencollection/favicon.svg" />
		<meta name="viewport" content="width=device-width" />
		<meta name="generator" content={Astro.generator} />
		<title>Create Binder</title>
		<script src="/Moltencollection/auth.js" is:inline></script>
		<style>
			* {
				margin: 0;
				padding: 0;
				box-sizing: border-box;
			}
			body {
				font-family: Arial, sans-serif;
				background-color: #1a1a1a;
				color: #fff;
				min-height: 100vh;
			}
			.nav {
				display: flex;
				gap: 0;
				background-color: #333;
			}
			.nav-btn {
				flex: 1;
				padding: 15px 20px;
				background-color: #333;
				color: white;
				text-decoration: none;
				text-align: center;
				border-right: 1px solid #555;
				transition: background-color 0.3s;
			}
			.nav-btn:hover {
				background-color: #555;
			}
			.nav-btn.active {
				background-color: #0066cc;
			}
			.nav-btn:last-child {
				border-right: none;
			}
			main {
				max-width: 1000px;
				margin: 0;
				padding: 20px;
			}
			.page-header {
				display: flex;
				justify-content: flex-start;
				align-items: center;
				gap: 12px;
				flex-wrap: wrap;
				margin-bottom: 10px;
			}
			.title-wrap {
				display: flex;
				align-items: center;
				gap: 10px;
				flex-wrap: wrap;
			}
			.page-title {
				font-size: 30px;
				font-weight: 700;
				margin: 0;
			}
			.back-btn {
				padding: 10px 12px;
				border-radius: 8px;
				border: 1px solid #41586b;
				background: #0d1117;
				color: #fff;
				font-size: 14px;
				cursor: pointer;
				text-decoration: none;
				display: inline-flex;
				align-items: center;
			}
			.back-btn:hover {
				background: #1b2633;
			}
			p {
				margin-bottom: 24px;
				color: #c8d0d8;
			}
			.form-card {
				background: #26323d;
				border: 1px solid #3a4a58;
				border-radius: 12px;
				padding: 20px;
			}
			label {
				display: block;
				font-weight: 700;
				margin-bottom: 8px;
			}
			input,
			textarea {
				width: 100%;
				padding: 12px;
				margin-bottom: 18px;
				border: 1px solid #55697b;
				border-radius: 8px;
				background: #1f2a33;
				color: #fff;
				font-size: 16px;
			}
			textarea {
				min-height: 100px;
				resize: vertical;
			}
			.actions {
				display: flex;
				gap: 12px;
			}
			button {
				padding: 12px 16px;
				border: none;
				border-radius: 8px;
				font-size: 16px;
				cursor: pointer;
			}
			button {
				background: #2ea043;
				color: #fff;
			}
			button:hover {
				background: #238636;
			}
			#status {
				margin-top: 14px;
				font-size: 14px;
				color: #9dd8a8;
			}
		</style>
	</head>
	<body>
		<nav class="nav">
			<a href="/Moltencollection/" class="nav-btn">Home</a>
			<a href="/Moltencollection/binders" class="nav-btn active">My Binders</a>
			<a href="/Moltencollection/deck" class="nav-btn">My Decks</a>
			<a href="/Moltencollection/collections" class="nav-btn">Collection</a>
		</nav>

		<main>
			<div class="page-header">
				<div class="title-wrap">
					<a class="back-btn" href="/Moltencollection/binders" title="Back to binders">&larr; Back</a>
					<h1 class="page-title">Create New Binder</h1>
				</div>
			</div>
			<p>Add a name for your binder.</p>

			<form id="new-binder-form" class="form-card">
				<label for="binder-name">Binder Name</label>
				<input id="binder-name" name="binderName" type="text" required maxlength="50" />

				<label for="binder-color">Color</label>
				<input id="binder-color" name="color" type="color" value="#2c3e50" />

				<div class="actions">
					<button type="submit">Create Binder</button>
				</div>
				<p id="status" aria-live="polite"></p>
			</form>
		</main>

		<script type="module">
		import { saveBinder } from '../../firestoreSync';
		const form = document.getElementById('new-binder-form');
		const status = document.getElementById('status');

		if (!form || !status) {
			throw new Error('Required form elements are missing.');
		}

		form.addEventListener('submit', async (event) => {
			event.preventDefault();
			const formData = new FormData(form);
			const binderName = String(formData.get('binderName') || '').trim();
			const colorInput = String(formData.get('color') || '#2c3e50');
			const color = /^#[0-9a-fA-F]{6}$/.test(colorInput) ? colorInput : '#2c3e50';

			if (!binderName) {
				status.textContent = 'Binder name is required.';
				return;
			}

<<<<<<< HEAD
			// Save binder to localStorage
			const STORAGE_KEY = 'mtgBinders';
			const binders = JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]');
			const newBinder = {
				id: Date.now(),
				name: binderName,
				color
			};
			binders.push(newBinder);
			localStorage.setItem(STORAGE_KEY, JSON.stringify(binders));
			status.textContent = 'Binder created. Redirecting...';
			setTimeout(() => {
				window.location.href = '/Moltencollection/binders';
			}, 500);
		});
=======
			form.addEventListener('submit', (event) => {
				event.preventDefault();
				const formData = new FormData(form);
				const binderName = String(formData.get('binderName') || '').trim();
				const colorInput = String(formData.get('color') || '#2c3e50');
				const color = /^#[0-9a-fA-F]{6}$/.test(colorInput) ? colorInput : '#2c3e50';

				if (!binderName) {
					status.textContent = 'Binder name is required.';
					return;
				}

				// Save binder to localStorage
				const STORAGE_KEY = 'mtgBinders';
				const binders = JSON.parse(localStorage.getItem(STORAGE_KEY) || '[]');
				const newBinder = {
					id: Date.now(),
					name: binderName,
					color
				};
				binders.push(newBinder);
				localStorage.setItem(STORAGE_KEY, JSON.stringify(binders));
				status.textContent = 'Binder created. Redirecting...';
				setTimeout(() => {
					window.location.href = '/Moltencollection/binders';
				}, 500);
			});
>>>>>>> 0cf59f6 (Fix syntax errors and restore localStorage binder creation logic)
		</script>
	</body>
</html>
